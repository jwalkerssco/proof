{
  "name": "proof",
  "version": "0.1.0",
  "private": true,
  "description": "Proof — proof of visit for field merchandisers. Start Visit, walk the plan, photo it, End Visit.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node build.js",
    "test": "node proof/test-proof.js && node lib/test-auth.js && node lib/test-sheet.js && node lib/test-db.js && node lib/test-ui.js",
    "dev": "node build.js && node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "lucide-react": "^0.452.0",
    "multer": "^2.4.0",
    "node-cron": "^3.0.3",
    "pg": "^8.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
